# 🚀 库存管理系统 - 快速开始指南

## ✅ 项目已创建完成！

你的Django项目结构已经搭建完毕，包含以下内容：

### 📁 项目结构
```
库存管理系统/
├── config/              ✅ Django配置
├── apps/                ✅ 应用模块
│   ├── authentication/  ✅ 用户认证（已完成）
│   ├── inventory/       ⚠️  库存管理（待实现）
│   ├── operations/      ⚠️  出入库操作（待实现）
│   ├── dashboard/       ⚠️  仪表盘（待实现）
│   ├── suppliers/       ⚠️  供应商（待实现）
│   ├── warehouses/      ⚠️  仓库（待实现）
│   └── reports/         ⚠️  报表（待实现）
├── common/              ✅ 公共模块
├── static/              ✅ 静态文件目录
├── media/               ✅ 媒体文件目录
├── templates/           ✅ 模板目录（含index.html）
├── requirements.txt     ✅ 依赖文件
├── .env                 ✅ 环境配置
├── manage.py            ✅ Django管理脚本
├── Dockerfile           ✅ Docker配置
├── docker-compose.yml   ✅ Docker编排
└── start.bat            ✅ 快速启动脚本
```

---

## 🎯 方式一：使用快速启动脚本（推荐）

### Windows用户

```bash
# 双击运行或在命令行执行
start.bat
```

这个脚本会自动：
1. 创建虚拟环境
2. 安装所有依赖
3. 初始化数据库
4. 启动开发服务器

---

## 🎯 方式二：手动启动

### 1. 创建虚拟环境

```bash
python -m venv venv
```

### 2. 激活虚拟环境

```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

### 4. 配置环境变量

`.env` 文件已自动创建，默认使用SQLite数据库，无需修改即可使用。

如需使用PostgreSQL，修改 `.env` 中的数据库配置。

### 5. 数据库迁移

```bash
# 创建迁移文件
python manage.py makemigrations

# 应用迁移
python manage.py migrate
```

### 6. 创建超级用户

```bash
python manage.py createsuperuser
```

按提示输入：
- 用户名
- 邮箱（可选）
- 密码

### 7. 启动开发服务器

```bash
python manage.py runserver
```

---

## 🌐 访问地址

启动成功后，访问以下地址：

| 功能 | 地址 | 说明 |
|------|------|------|
| **前端界面** | http://localhost:8000/ | 库存管理系统主界面 |
| **API文档** | http://localhost:8000/swagger/ | Swagger API文档 |
| **ReDoc文档** | http://localhost:8000/redoc/ | ReDoc API文档 |
| **管理后台** | http://localhost:8000/admin/ | Django管理后台 |

---

## 🐳 使用Docker部署

### 1. 启动所有服务

```bash
docker-compose up -d
```

这会启动：
- PostgreSQL数据库
- Redis缓存
- Django应用
- Nginx反向代理

### 2. 查看日志

```bash
docker-compose logs -f web
```

### 3. 执行数据库迁移

```bash
docker-compose exec web python manage.py migrate
```

### 4. 创建超级用户

```bash
docker-compose exec web python manage.py createsuperuser
```

### 5. 停止服务

```bash
docker-compose down
```

---

## 📝 下一步开发

### 当前状态

✅ **已完成**：
- 项目基础架构
- 用户认证模块（登录、注册、JWT）
- 统一响应格式
- 分页和异常处理
- API文档集成
- Docker部署配置

⚠️ **待实现**：
- 库存管理模块（物品CRUD）
- 出入库操作模块
- 仪表盘统计
- 供应商管理
- 仓库管理
- 报表分析

### 实现核心功能

我已经为你创建了所有模块的基础文件，你需要：

1. **实现数据模型** - 在各模块的 `models.py` 中定义数据库模型
2. **实现序列化器** - 在 `serializers.py` 中定义API序列化器
3. **实现视图** - 在 `views.py` 中实现业务逻辑
4. **配置路由** - 在 `urls.py` 中配置API路由

参考文档：
- [后端开发文档.md](./后端开发文档.md) - 详细的开发指南
- [API文档.md](./API文档.md) - API接口规范

---

## 🔧 常用命令

### 数据库相关

```bash
# 创建迁移
python manage.py makemigrations

# 应用迁移
python manage.py migrate

# 回滚迁移
python manage.py migrate app_name migration_name

# 查看SQL语句
python manage.py sqlmigrate app_name migration_number
```

### 用户管理

```bash
# 创建超级用户
python manage.py createsuperuser

# 修改用户密码
python manage.py changepassword username
```

### 静态文件

```bash
# 收集静态文件
python manage.py collectstatic
```

### 开发工具

```bash
# 进入Django Shell
python manage.py shell

# 运行测试
python manage.py test

# 检查项目问题
python manage.py check
```

---

## 🧪 测试API

### 使用curl测试登录

```bash
# 注册用户
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "testpass123",
    "password_confirm": "testpass123"
  }'

# 登录
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "testpass123"
  }'

# 获取用户信息（需要token）
curl -X GET http://localhost:8000/api/auth/profile/ \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

---

## ❓ 常见问题

### 1. 端口被占用

```bash
# 使用其他端口启动
python manage.py runserver 8080
```

### 2. 数据库连接错误

检查 `.env` 文件中的数据库配置是否正确。

### 3. 静态文件404

```bash
# 开发环境下Django会自动处理静态文件
# 如果有问题，运行：
python manage.py collectstatic
```

### 4. 模块导入错误

确保虚拟环境已激活：
```bash
venv\Scripts\activate
```

---

## 📚 参考资料

- [Django官方文档](https://docs.djangoproject.com/)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [后端开发文档](./后端开发文档.md)
- [API文档](./API文档.md)

---

## 💡 提示

1. **开发环境**使用SQLite，无需额外配置
2. **生产环境**建议使用PostgreSQL
3. 所有API都需要JWT Token认证（除了登录和注册）
4. 查看 `swagger/` 获取完整的API文档
5. 使用 `admin/` 后台快速管理数据

---

## 🎉 开始开发

现在你可以：

1. ✅ 访问 http://localhost:8000/ 查看前端界面
2. ✅ 访问 http://localhost:8000/swagger/ 查看API文档
3. ✅ 访问 http://localhost:8000/admin/ 管理后台
4. ⚠️ 开始实现各模块的业务逻辑

祝开发顺利！🚀
